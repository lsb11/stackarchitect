# Free Shopify automation guides and tools. Server-side tracking (Meta CAPI,
# Google Enhanced Conversions, TikTok Events API), inventory, email and live
# P&L — built on free tiers. Maintained by Luke Sandelands.

# ─────────────────────────────────────────────────────────────
# AI / answer-engine crawlers — explicitly permitted.
# These are allow-by-default under the wildcard below, but naming
# them removes any ambiguity for crawlers that look for a specific
# user-agent block before falling through to *.
# ─────────────────────────────────────────────────────────────
User-agent: GPTBot
User-agent: ChatGPT-User
User-agent: OAI-SearchBot
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: Bingbot
User-agent: cohere-ai
User-agent: MistralAI-User
User-agent: meta-externalagent
Allow: /
Disallow: /go/

# ─────────────────────────────────────────────────────────────
# Everyone else
# ─────────────────────────────────────────────────────────────
User-agent: *
Allow: /

Disallow: /go/

# /embed/ IS DELIBERATELY NOT DISALLOWED (2026-09-18)
#
# The three /embed/* pages carry <meta name="robots" content="noindex">, and
# they were ALSO disallowed here. Those two instructions contradict each other:
# a crawler forbidden to fetch a URL cannot read the noindex inside it, so the
# URL sits in limbo — eligible to be listed from third-party links, never
# eligible to be dropped on the site's own instruction. GSC reported two URLs
# under "Blocked by robots.txt" and /embed/* is one of only two disallowed
# paths on the site.
#
# The noindex is the stronger and more precise signal, so it is the one kept.
# Letting crawlers fetch these pages is what makes it enforceable. Do not
# re-add the Disallow without first removing the noindex — pick one.
#
# /go/ stays disallowed: those are affiliate cloaks that 302 off-site, they
# carry no noindex of their own, and there is nothing at the URL to index.

# Tracking parameters are deliberately NOT disallowed.
#
# WHY THIS CHANGED (2026-08-17)
# These six Disallow rules were blocking parameterised URLs to "preserve crawl
# budget". On a 58-page site crawl budget is not a constraint, and the rules were
# doing real harm: GSC reported https://stackarchitect.xyz/?ref=producthunt in
# "Blocked by robots.txt". That is an inbound Product Hunt link. Because robots.txt
# forbade crawling it, Google could not read the page's rel=canonical and so could
# not consolidate that link's equity onto the homepage. The same applied to any
# inbound link carrying a utm_* parameter.
#
# Google's own guidance is to let parameterised URLs be crawled and to deduplicate
# with rel=canonical, never to block them in robots.txt when the goal is
# consolidation. Every indexable page here self-canonicalises correctly (verified
# 2026-08-17, zero mismatches across 59 pages), so canonical handles duplication
# and link equity now flows to the canonical URL.

Sitemap: https://stackarchitect.xyz/sitemap-index.xml
