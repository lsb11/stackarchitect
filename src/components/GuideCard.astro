---
interface Props {
  badge?: string;
  badgeType?: 'urgent' | 'new' | 'comparison' | 'hot' | 'default';
  category: string;
  title: string;
  description: string;
  readTime: number;
  href: string;
}

const { badge, badgeType = 'default', category, title, description, readTime, href } = Astro.props;

const badgeStyles: Record<string, string> = {
  urgent: 'bg-red-900/50 text-red-400 border-red-700',
  new: 'bg-[#4ade80]/10 text-[#4ade80] border-[#4ade80]/30',
  comparison: 'bg-blue-900/50 text-blue-400 border-blue-700',
  hot: 'bg-orange-900/50 text-orange-400 border-orange-700',
  default: 'bg-[#27272a] text-[#52525b] border-[#1f1f1f]',
};
---

<a
  href={href}
  class="block border border-[#1f1f1f] bg-[#0a0a0a] p-5 hover:border-[#4ade80]/30 hover:bg-[#0f0f0f] transition-all group relative"
>
  {
    badge && (
      <div
        class={`absolute top-3 right-3 text-xs px-2 py-0.5 border uppercase tracking-wide font-bold ${badgeStyles[badgeType]}`}
      >
        {badge}
      </div>
    )
  }
  <div class="text-xs text-[#52525b] uppercase tracking-widest mb-2">{category}</div>
  <h3 class="text-sm font-bold text-white group-hover:text-[#4ade80] transition-colors mb-2 pr-16">
    {title}
  </h3>
  <p class="text-xs text-gray-500 leading-relaxed mb-4">{description}</p>
  <div class="flex items-center justify-between">
    <span class="text-xs text-[#52525b]">{readTime} min read</span>
    <span class="text-xs text-[#4ade80] group-hover:translate-x-0.5 transition-transform">→</span>
  </div>
</a>
